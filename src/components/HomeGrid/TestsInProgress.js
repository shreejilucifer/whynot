import Card from '../Card';
import Empty from '../Empty';

export default function TestsInProgress() {
	return (
		<Card title="Tests in Progress">
			<Empty msg="No tests in progress." />
		</Card>
	);
}
