import Card from '../Card';
import Empty from '../Empty';

export default function ScheduledTests() {
	return (
		<Card title="Scheduled Tests">
			<Empty msg="Scheduled scan not found" />
		</Card>
	);
}
